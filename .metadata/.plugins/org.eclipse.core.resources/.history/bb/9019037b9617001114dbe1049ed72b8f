<%@page import="java.util.Calendar"%>
<%@ page contentType="text/html; charset=UTF-8"%>
<%
	String sYear = request.getParameter("year");
	String sMonth = request.getParameter("month");

	Calendar cal = Calendar.getInstance();
	
	int nowYear = cal.get(Calendar.YEAR);
	int nowMonth = cal.get(Calendar.MONTH)+1;
	int nowDay = cal.get(Calendar.DAY_OF_WEEK);
	int nowDate = cal.get(Calendar.DATE);
	
	int selectYear = (sYear == null) ? nowYear : Integer.parseInt(sYear);
    int selectMonth = (sMonth == null) ? nowMonth : Integer.parseInt(sMonth);
	
    cal.set(selectYear, selectMonth - 1, 1);
    
	String[] weekNames = {"일요일", "월요일", "화요일", "수요일", "목요일", "금요일", "토요일"};
	int startDay = cal.get(Calendar.DAY_OF_WEEK);     // 1일의 요일 (1:일 ~ 7:토)
    int lastDay = cal.getActualMaximum(Calendar.DATE); // 해당 월의 마지막 날짜
	
	// 달력 그리기
	String calStr ="";
	
	calStr += "<table border='1'>";
	calStr += "<tr>";
	for(int i = 0; i<weekNames.length; i++){
		calStr += "<th>" + weekNames[i] +"</th>";
	}
	calStr += "</tr><tr>";
	for(int i = 1 ; i<startDay;i++)
		calStr +="<td></td>";
	
	int week = startDay;
	for(int i = 1; i<=lastDay; i++, week++)
	{
		calStr += "<td>"+ i + "</td>";
		if(week%7==0){
			calStr += "</tr><tr>"; 
		}
	}
	
	for(int i=1; i<=week; i++, week++)
	{
		if(week%7==0)
			break;
		calStr += "<td></td>";
	}
	calStr += "</tr>";
	calStr += "</table>";
	

%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>SendAndReceive12.jsp</title>
<link rel="stylesheet" type="text/css" href="css/main.css" />
<style type="text/css">
form{height: 50px;  width: 200px;}
form>*{float: left; line-height: 50px;}
p{background-color: #fff; display:inline-block; height: 50px; margin: 0 2px;}
button{background-color: #fff; border:none; font-size: 20px; cursor: pointer;}
</style>
<script type="text/javascript">

	let yearStr = document.getElementById("year").value;
	let monthStr = document.getElementById("month").value;
	
	function changeDate(num){
		
		
		
		for(let i = 1; i<=12; i++)
		{
				
		}
		
		
		
		
	}

	
	function formChange(obj)
	{
		obj.submit();
	}
</script>
</head>
<body>
<div>
	<h1>JSP를 이용한 데이터 송수신 실습 12</h1>
	<p>SendAndReceive12.jsp● → SendAndReceive12.jsp●</p>
	<hr />
</div>
<!-- 
	○ 데이터 송수신 실습
	
		- 사용자가 선택한 연도와 월에 대해 달력을 구성하는 JSP 페이지를 구성한다.
		- 단, submit 버튼 없이 이벤트 처리를 할 수 있도록 하낟.
		- 전송한 내용을 자신 페이지로 수신하여 출력해주는 형태의 단독 페이지로 구성한다.
		
		페이지 레이아웃
		-------------------------------------------------------
		
		       ◀ 2026년 3월 ▶
		  ----------------------------
		  ---- 해당 년 월의 달력 -----
		  ----------------------------
		  ----------------------------
		-------------------------------------------------------
		
		- ◀▶ 클릭 시 해당 년 월의 달력을 출력해주는 형태의 페이지로 구성한다.
		- 사용자 최초 요청 주소
	      http://localhost:3306/WebApp06/SendAndReceive12.jsp
	      
	   - 물리적 파일 구성
	     ·SendAndReceive12.jsp
	
 -->
<div>
	<form method="post">
		<button class="prev" onchange="formChange(this.form)" onclick="changeDate(-1)">◀</button>
		<div class="dateWrap">		
			<input type="hidden" id="year"><%= selectYear %>년
			<input type="hidden" id="month"><%= selectMonth %>월
		</div>
		<button class="next" onchange="formChange(this.form)" onclick="changeDate(1)">▶</button>
	</form>
</div>
<br><br>
<div>
달력 그리기
<%= calStr %>
</div>
</body>
</html>